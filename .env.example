# Vercel / production Redis. Use the connection URL from your Redis provider.
REDIS_URL=

# Optional: only needed if using a separate frontend origin.
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

SECRET_KEY=change-this-in-production
SESSION_TTL=3600
MAX_FILE_SIZE=10485760
